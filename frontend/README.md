# Tutor Scheduler — frontend

Vite + React + TypeScript app. See the **[project README](../README.md)** for architecture diagrams, API overview, and how the whole stack fits together.

## Quick start

```bash
npm install
npm run dev
```

Requires the backend on `http://localhost:3000` for live session/bill data (see `src/store/api.tsx`).
